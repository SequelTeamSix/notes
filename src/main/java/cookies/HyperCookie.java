package cookies;

public class HyperCookie extends Cookie{
    private int dimensions;
    public HyperCookie(){
        dimensions = 4;
    }
//    we can still use hyperCookie.setEdible, for instance, because that method has been inherited from cookie
}
